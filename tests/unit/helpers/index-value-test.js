import { indexValue } from 'super-rentals/helpers/index-value';
import { module, test } from 'qunit';

module('Unit | Helper | index value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = indexValue([42]);
  assert.ok(result);
});
