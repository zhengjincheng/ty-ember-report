import { ifTrue } from 'super-rentals/helpers/if-true';
import { module, test } from 'qunit';

module('Unit | Helper | if true');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifTrue([42]);
  assert.ok(result);
});
